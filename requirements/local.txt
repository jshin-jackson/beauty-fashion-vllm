# 로컬 (M2 Pro) 환경 의존성
# base.txt 포함
-r base.txt

# vLLM — Apple Silicon 빌드 방법
#
# ⚠️  직접 pip install vllm 하면 AppleClang 21에서 C++ 컴파일 오류 발생
#     (static_assert 체인 비교 구문 에러 처리)
#
# 해결방법 A (권장): 컴파일러 플래그로 해결
#   scripts/install_vllm_local.sh 실행
#
# 해결방법 B: OrbStack + Linux ARM64 컨테이너에서 vLLM 실행
#   scripts/run_vllm_orbstack.sh 참고
#
# 직접 설치 시도:
#   CXXFLAGS="-Wno-parentheses" CFLAGS="-Wno-parentheses" pip install vllm
vllm>=0.6.0

# Jupyter (노트북 실행용)
jupyter>=1.0.0
ipykernel>=6.0.0
